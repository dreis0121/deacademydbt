macros:
  - name: cents_to_dollars
    description: A macro that converts cents to dollars and round by default to 2 or to number input
    arguments:
      - name: column_name
        type: string
        description: The name of the column you want to convert
      - name: decimal
        type: integer
        description: How many decimal points to use when rounding